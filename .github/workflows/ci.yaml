name: Test

env:
  TESTFLAGS: -v -p 1

on:
  workflow_dispatch: # Allow the pipeline to be run manually.
  push:
    branches:
    - master
    - crl-release-*
  pull_request:
    branches:
    - master
    - crl-release-*

jobs:

  linux-race:
    name: go-linux-race
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: 7d9a5b2e29d53d4296535185c61d46f4a3a82022

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: "1.18"

    - run: make testrace TAGS=
