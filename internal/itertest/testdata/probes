# Create a probe that ignores the inner iterator's return values
# and instead returns a specific KV. It should apply to all internal
# iterator operations because there's no conditional logic.

new
(ReturnKV "bar#2,SET" "value")
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
----
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value

# Create probes to exercise the UserKey predicate. On 2 SeekGE Index return a KV
# with user key "bar"; on other ops return a KV with user key "foo". Then if
# the returned KV's user key is "bar", override the result with a specific KV.
# In this case "ok"

new
(If (And OpSeekGE (OnIndex 2)) (ReturnKV "bar#2,SET" "value") (ReturnKV "foo#1,SET" "value"))
(If (UserKey "bar") (ReturnKV "ok#1,SET" "ok") noop)
----

iter
first
next
seek-ge foo
prev
seek-ge foo
next
seek-ge foo
----
foo#1,SET:value
foo#1,SET:value
foo#1,SET:value
foo#1,SET:value
foo#1,SET:value
foo#1,SET:value
ok#1,SET:ok

# Create an identical probe as above, and then wrap it in a "noop" probe.
# A noop probe does nothing; it passes through the child iterator's results
# verbatim, so the iteration results should be the same.

new
(ReturnKV "bar#2,SET" "value")
noop
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
----
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value

# Again, create an iterator with similar probes but this time with an additional
# probe level that ignores the inner return values and injects errors.

new
(ReturnKV "bar#2,SET" "value")
noop
ErrInjected
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
----
err=injected error
err=injected error
err=injected error
err=injected error
err=injected error
err=injected error

# Create a probe that returns a specific KV on seeks and errors on
# Next and Prev.

new
(If (Or OpNext OpPrev) ErrInjected (ReturnKV "bar#2,SET" "value"))
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
----
bar#2,SET:value
err=injected error
bar#2,SET:value
bar#2,SET:value
bar#2,SET:value
err=injected error

# Create a probe that returns an error on the 3rd seek-ge.

new
(If (And OpSeekGE (OnIndex 2)) ErrInjected (ReturnKV "ok#1,SET" "ok"))
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
seek-ge bar
seek-ge bax
seek-ge bop
----
ok#1,SET:ok
ok#1,SET:ok
ok#1,SET:ok
ok#1,SET:ok
ok#1,SET:ok
ok#1,SET:ok
ok#1,SET:ok
err=injected error
ok#1,SET:ok

# Create a probe that always returns nil, except for on the 3rd seek-ge.

new
(If (And OpSeekGE (OnIndex 2)) (ReturnKV "ok#1,SET" "ok") Nil)
----

iter
first
next
seek-ge foo
seek-lt bar
last
prev
seek-ge bar
seek-ge bax
seek-ge bop
----
.
.
.
.
.
.
.
ok#1,SET:ok
.
