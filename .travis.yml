language: go

install:
  - go get -t -v ./...
  - go get -v golang.org/x/lint/golint

matrix:
  include:
    - name: "go1.12.x-windows"
      go: 1.12.x
      os: windows
      script: go test ./vfs/...

notifications:
  email:
    recipients:
      - peter@cockroachlabs.com
    on_success: never
    on_failure: always
